import { ResourceStringsKeys } from '@config/ConfigsKeys';

export interface UseSignupKey {
    ctaFreeTrialKey: ResourceStringsKeys;
    ctaGetStarted?: ResourceStringsKeys;
    ctaSignUpKey: ResourceStringsKeys;
}
