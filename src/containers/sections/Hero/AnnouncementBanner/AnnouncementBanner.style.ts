import { css } from '@emotion/core';

export const announcementBannerStyle = css(
    {
        display: 'inline-block',
        marginTop: 0,
        marginBottom: 24,
        whiteSpace: 'pre-wrap',
    },
);
