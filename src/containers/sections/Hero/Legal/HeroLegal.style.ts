import { bodyFontStyles } from '@components/Fonts/Typography.style';
import { css } from '@emotion/core';

export const noteCopyStyle = css(
    {
        marginTop: 24,
        marginBottom: 64,
        whiteSpace: 'pre-wrap',
    },
    bodyFontStyles.bodyRegular3,
);
